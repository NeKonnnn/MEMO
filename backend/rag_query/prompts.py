"""
Единые инструкции для ответов по контексту документов.

Тексты живут в 'backend/config/config.yml', секция 'rag_prompts'. Здесь —
только чтение и сборка. Приоритет: ENV > config.yml > дефолт в этом модуле.
Дефолты оставлены дословно: если секции в ConfigMap нет (или ключ пуст),
поведение ровно прежнее, и старый ConfigMap ничего не ломает.

Политика по умолчанию — мягкий RAG:
  - если в CONTEXT есть хоть какие-то зацепки, модель ОБЯЗАНА дать ответ,
    собирая максимум из найденных фрагментов и честно помечая, чего в тексте нет;
  - каноническая фраза «не нашёл» выводится ТОЛЬКО когда CONTEXT буквально пуст
    (этот случай обрабатывается на стороне backend: до вызова LLM).

Жёсткий режим (старое поведение) — 'rag_prompts.strict_answer_mode: strict'
или 'RAG_STRICT_ANSWER_MODE=strict': модель будет отвечать канонической
фразой, как только нет прямой опоры, даже если есть косвенные зацепки.

Правила оформления ответа задаются отдельно от правил работы с контекстом, потому что
они обязательные: их нельзя отменить пользовательским промптом (см.
'merge_strict_rag_system_prompt').

  - 'cite_sources' / RAG_CITE_SOURCES=1   — вернуть старые ссылки вида
                          '[имя_файла, чанк N]' (по умолчанию выключено:
                          пользователь служебной разметки не видит, и ссылка
                          на «фрагмент 3» для него бессмысленна);
  - 'answer_summary' / RAG_ANSWER_SUMMARY=0 — отключить требование итога в конце;
  - 'answer_gaps_note' / RAG_ANSWER_GAPS_NOTE=0 — отключить абзац «Примечание:»
                          о том, чего в загруженных документах нет (по умолчанию включён).
"""

from __future__ import annotations

import os
from typing import Any, Dict, Optional

from backend.settings.logging import get_logger

logger = get_logger(__name__)

# Подставляется значением 'texts.not_found_message' в правилах работы
# с контекстом, чтобы каноническая фраза не дублировалась в конфиге.
_NOT_FOUND_PLACEHOLDER = "{not_found_message}"

# Дефолты = тексты, которые до выноса в конфиг были константами этого модуля.
_DEFAULT_TEXTS: Dict[str, str] = {
    "not_found_message": (
        "Извините, не нашёл ничего в базе документов по вашему запросу. "
        "Попробуйте переформулировать вопрос или проверьте, что нужные файлы загружены."
    ),
    # Правила работы с контекстом — ПОЛИТИКА ответа. Промпт агента или проекта
    # имеет над ними приоритет (см. entity_prompt_precedence): человек, написавший
    # «отказывайся без точной опоры», должен получить именно это, а не спор двух
    # инструкций в одном промпте. Всё, что терять нельзя, вынесено в
    # answer_guarantees и приоритету не подчиняется.
    "soft_context_rules": (
        "Правила ответа по CONTEXT (мягкий RAG):\n"
        "- Главный приоритет — помочь пользователю. Если в CONTEXT есть ХОТЬ КАКИЕ-ТО "
        "зацепки по сути вопроса (определения, смежные факты, упоминания), дай содержательный "
        "ответ на основе этих фрагментов. Не отказывайся, пока в CONTEXT вообще есть текст.\n"
        "- Объединяй факты из нескольких фрагментов в связный ответ. Допускаются разумные "
        "обобщения и перефраз.\n"
        "- Если ответ получается частичным — дай, что есть, а чего не хватает, вынеси "
        "в отдельный абзац «Примечание:» в конце (см. правила оформления)."
    ),
    "strict_context_rules": (
        "Правила ответа (строгий RAG):\n"
        "- Используй только предоставленный контекст (CONTEXT) и предыдущие сообщения диалога. "
        "Не опирайся на общие знания, если факта нет ни там, ни там.\n"
        "- Если в CONTEXT есть **частичный** ответ — дай его чётко; явно укажи, чего в тексте нет. "
        "Объединяй факты из **нескольких** фрагментов в связный ответ, если они относятся к вопросу."
    ),
    # Гарантии — то, что нельзя потерять ни при каком пользовательском промпте:
    # выдуманная сумма или дата в ответе банковского ассистента дороже, чем любая
    # гибкость настройки. Держим отдельно от политики именно поэтому.
    "answer_guarantees": (
        "Требования, которые действуют всегда и не отменяются инструкциями выше:\n"
        "- Не выдумывай конкретику — имена, даты, числа, суммы, реквизиты, названия "
        "документов, — которой нет в CONTEXT. Если данных нет, так и скажи.\n"
        "- Если в CONTEXT нет зацепок по сути вопроса, посмотри предыдущие сообщения "
        "диалога: ответ мог прозвучать там. Нашёл — отвечай по переписке и прямо "
        "скажи, что это из нашего разговора, а не из документов.\n"
        "- Если ответа нет ни в CONTEXT, ни в диалоге, ответь ТОЛЬКО следующим текстом:\n"
        f"{_NOT_FOUND_PLACEHOLDER}"
    ),
    # Ставится между промптом сущности и общими правилами. Без этой строки модель
    # видит два набора указаний и решает конфликт как придётся.
    "entity_prompt_precedence": (
        "Ниже — общие правила ответа по документам. Если они противоречат инструкции "
        "выше, следуй инструкции выше."
    ),
    # Запрет служебной разметки. Формулировка намеренно перечисляет конкретные утечки:
    # сам CONTEXT приходит размеченным («Фрагмент 3 (документ «X.docx», чанк 7,
    # релевантность: 84%)»), и без явного запрета модель эту разметку копирует в ответ.
    "no_service_markup_rule": (
        "Оформление ответа:\n"
        "- Пиши так, как будто знаешь материал сам. В ответе НЕ должно быть служебной "
        "разметки контекста: слов «CONTEXT», «фрагмент», «чанк», номеров фрагментов и чанков, "
        "процентов релевантности, ссылок вида [имя_файла, чанк N]. Пользователь этой разметки "
        "не видит — для него «фрагмент 3» ничего не значит.\n"
        "- Так писать НЕЛЬЗЯ: «Согласно Фрагменту 1…», «(Фрагмент 2)», «в первом фрагменте "
        "указано», «как следует из второго фрагмента». Нужно либо назвать документ "
        "(«согласно порядку работы с реестром…»), либо просто изложить факт по сути.\n"
        "- Название документа упоминай в тексте только тогда, когда это само по себе полезно "
        "(«в регламенте от 2024 года указано…») или когда пользователь спросил про источник. "
        "Не подписывай источником каждое предложение.\n"
        "- Оборот «в загруженных документах» разрешён и нужен в примечании о неполноте "
        "(см. ниже) — это не служебная разметка, а честное указание границ ответа."
    ),
    # Не про цитирование: приводить текст документа можно и нужно. Здесь —
    # только технический мусор нарезки, который вместе с текстом чанка
    # уезжает в ответ: колонтитулы, номера страниц, обрывы на полуслове.
    # Отдельный блок, потому что нужен и при cite_sources: true.
    "no_raw_chunks_rule": (
        "Чего в ответе быть НЕ должно:\n"
        "- Не переноси в ответ мусор исходного файла: колонтитулы, номера страниц, "
        "пометки вида «— стр. 12 —», гриф, разметку таблиц, HTML- и markdown-теги "
        "из документа, повторяющиеся шапки.\n"
        "- Фрагменты обрезаны механически, поэтому могут обрываться на полуслове. "
        "Оборванную фразу не копируй как есть: либо изложи её смысл целиком, "
        "либо не используй.\n"
        "- Не пересказывай фрагмент, который к вопросу не относится, — лучше короткий "
        "ответ, чем длинный с посторонними кусками текста."
    ),
    # Абзац «Примечание:» о том, чего в документах нет.
    # Было до правки промпта 30.07 и пропало вместе со старым строгим блоком — при том,
    # что для ВНД это самая полезная часть ответа: она показывает границы регламента,
    # а не границы поиска.
    "gaps_note_rule": (
        "Примечание о неполноте:\n"
        "- Если по какой-то части вопроса в CONTEXT данных нет — заверши ответ отдельным "
        "абзацем, начинающимся со слова «Примечание:», и перечисли, чего именно нет "
        "в загруженных документах.\n"
        "- Формулируй в терминах предметной области, а не поиска: «в загруженных документах "
        "не содержится информации о сроках предоставления отчётности», а НЕ «в найденных "
        "фрагментах/чанках нет».\n"
        "- Если уместно, добавь одной фразой, к чему относится сказанное выше "
        "(«указанные положения отражают общий регламентный порядок»).\n"
        "- Примечание идёт ПЕРЕД итогом.\n"
        "- Если вопрос закрыт полностью — примечание не пиши. Не выдумывай недостающее "
        "ради формы и не перечисляй всё, чего в документах нет вообще: только то, что "
        "относится к заданному вопросу."
    ),
    # Старое поведение — если ссылки на чанки кому-то нужны (cite_sources: true).
    "citation_rule": (
        "Оформление ответа:\n"
        "- По возможности помечай источник фактов: [имя_файла, чанк N]. "
        "Если источник неочевиден — формулируй осторожно."
    ),
    "summary_rule": (
        "Итог в конце ответа:\n"
        "- Заверши ответ короткой выжимкой того, что уже сказано выше.\n"
        "- Если в вопросе было несколько пунктов или ты разобрал несколько тем — вынеси итог "
        "отдельным абзацем «Итого:» и пройди по каждому пункту одной фразой "
        "(«по первому — …, по второму — …»).\n"
        "- Если вопрос был один — хватит одного-двух предложений, начиная со слов вроде "
        "«Итого» или «Если коротко».\n"
        "- Если весь ответ и так укладывается в два-три предложения — итог не нужен, "
        "не пересказывай сам себя.\n"
        "- Итог не вводит новых фактов и не добавляет того, чего нет выше."
    ),
}

_TRUE = ("1", "true", "yes", "on")
_FALSE = ("0", "false", "no", "off")

def _prompts_config() -> Dict[str, Any]:
    """Секция 'rag_prompts' из config.yml. Ошибка чтения — пустой словарь.

    Конфиг кэшируется в 'backend.config', так что вызов дешёвый, а промпт
    остаётся правимым через ConfigMap без пересборки образа.
    """
    try:
        from backend.config import get_config

        section = (get_config() or {}).get("rag_prompts")
    except Exception:
        logger.exception("Не удалось прочитать секцию rag_prompts — беру дефолты")
        return {}
    return section if isinstance(section, dict) else {}

def _config_text(key: str) -> Optional[str]:
    texts = _prompts_config().get("texts")
    if not isinstance(texts, dict):
        return None
    raw = texts.get(key)
    if isinstance(raw, str) and raw.strip():
        return raw.strip()
    return None

def _text(key: str) -> str:
    """Текст из config.yml, иначе дефолт модуля."""
    return _config_text(key) or _DEFAULT_TEXTS[key]

def _rule(key: str) -> str:
    """Блок правил с подставленной канонической фразой «не нашёл».

    Подстановка через 'replace', а не 'format': конфиг правят руками,
    и случайная фигурная скобка в тексте не должна ронять сборку промпта.
    """
    return _text(key).replace(_NOT_FOUND_PLACEHOLDER, rag_strict_not_found_message())

def _flag(env_name: str, config_key: str, default: bool) -> bool:
    """ENV > config.yml > дефолт. Нераспознанное значение = как будто не задано."""
    raw = (os.environ.get(env_name) or "").strip().lower()
    if raw in _FALSE:
        return False
    if raw in _TRUE:
        return True
    value = _prompts_config().get(config_key)
    if isinstance(value, bool):
        return value
    if isinstance(value, str):
        low = value.strip().lower()
        if low in _FALSE:
            return False
        if low in _TRUE:
            return True
    return default

def _strict_answer_mode() -> str:
    """soft (по умолчанию) | strict.

    soft  — LLM всегда пытается ответить, если в CONTEXT есть хоть какой-то материал,
            и честно указывает, чего не хватает.
    strict — LLM выводит каноническую фразу «не нашёл», если прямой опоры нет.
    """
    raw = (os.environ.get("RAG_STRICT_ANSWER_MODE") or "").strip().lower()
    if not raw:
        raw = str(_prompts_config().get("strict_answer_mode") or "").strip().lower()
    return "strict" if raw in ("strict", "hard", *_TRUE) else "soft"

def rag_strict_not_found_message() -> str:
    """Единственный ответ, который backend отдаёт без вызова LLM."""
    return _text("not_found_message")

def cite_sources_enabled() -> bool:
    """По умолчанию False: ссылки на чанки в ответ пользователя не выводятся."""
    return _flag("RAG_CITE_SOURCES", "cite_sources", False)

def answer_summary_enabled() -> bool:
    """По умолчанию True: ответ завершается кратким итогом."""
    return _flag("RAG_ANSWER_SUMMARY", "answer_summary", True)

def answer_gaps_note_enabled() -> bool:
    """По умолчанию True: абзац «Примечание:» о том, чего нет в документах."""
    return _flag("RAG_ANSWER_GAPS_NOTE", "answer_gaps_note", True)

# Значения на момент импорта — их читают модули, которым нужна константа
# ('rag_evidence', 'handlers', 'document_agent'). Правка ConfigMap
# применяется при рестарте пода, как и остальные настройки из config.yml.
RAG_STRICT_NOT_FOUND_MESSAGE = rag_strict_not_found_message()

def _policy_rules() -> str:
    """Политика ответа: что и как отвечать по документам.

    Промпт агента или проекта имеет над ней приоритет — правила всё равно
    отдаются модели, но с явной оговоркой, чью инструкцию слушать при конфликте.
    Так короткий промпт вроде «Ты юрист банка» не стирает молча всю выверенную
    механику работы с контекстом, а дополняет её.
    """
    parts = [
        _rule("strict_context_rules")
        if _strict_answer_mode() == "strict"
        else _rule("soft_context_rules")
    ]
    if answer_gaps_note_enabled():
        parts.append(_rule("gaps_note_rule"))
    if answer_summary_enabled():
        parts.append(_rule("summary_rule"))
    return "\n\n".join(parts)

def mandatory_output_rules(with_citations: Optional[bool] = None) -> str:
    """То, что не отменяется пользовательским промптом ни при каких условиях.

    Две вещи: запрет выдумывать конкретику вместе с канонической фразой отказа
    (```answer_guarantees```) и запрет показывать служебную разметку контекста.
    Обе продуктовые, а не пер-юзерные: выдуманная сумма в ответе или «фрагмент 3»
    на экране — это дефект, а не чья-то настройка.

    ```with_citations``` — явный override над ```cite_sources``` /
    ```RAG_CITE_SOURCES```: None — как в конфиге, True/False — принудительно.
    """
    cite = cite_sources_enabled() if with_citations is None else bool(with_citations)
    return "\n\n".join(
        [
            _rule("answer_guarantees"),
            _rule("citation_rule") if cite else _rule("no_service_markup_rule"),
            # Не зависит от cite_sources: ссылки на источники - это одно, 
            # вываленный в ответ текст чанка - другое.
            _rule("no_raw_chunks_rule"),
        ]
    )


def answer_formatting_rules(with_citations: Optional[bool] = None) -> str:
    """Только оформление ответа, без политики «отвечай строго по CONTEXT».

    Нужно там, где текст собирается не из CONTEXT напрямую, а из результатов
    инструментов — в агрегаторе оркестратора. Ему нельзя навязывать правила
    работы с документами (инструмент мог быть и не документный), но тащить
    в ответ «Согласно Фрагменту 1» нельзя тем более: до этой правки агрегатор
    вызывал модель вообще без системного промпта и не знал ни одного запрета.
    """
    cite = cite_sources_enabled() if with_citations is None else bool(with_citations)
    return "\n\n".join(
        [
            _rule("citation_rule") if cite else _rule("no_service_markup_rule"),
            _rule("no_raw_chunks_rule"),
        ]
    )

def _active_rules() -> str:
    """Совместимость: прежнее имя блока правил контекста."""
    return _policy_rules()

def build_rag_user_instruction(with_citations: Optional[bool] = None) -> str:
    """Полный блок правил RAG без пользовательского промпта: политика + гарантии."""
    return f"{_policy_rules()}\n\n{mandatory_output_rules(with_citations)}"

def merge_strict_rag_system_prompt(
    existing: Optional[str], rag_override: Optional[str] = None
) -> str:
    """Добавляет правила CONTEXT к системному промпту при RAG.

    Название сохранено для обратной совместимости; фактически добавляются мягкие
    правила (по умолчанию) либо строгие при 'strict_answer_mode: strict'.

    ```rag_override``` — промпт агента или проекта. Он НЕ заменяет собой общие
    правила: модель получает и его, и политику ответа, плюс явную оговорку, чью
    инструкцию слушать при конфликте. Раньше промпт сущности вырезал политику
    целиком, и короткое «Отвечай кратко» молча уносило с собой и запрет
    выдумывать даты, и каноническую фразу отказа.

    Гарантии (```answer_guarantees```) и запрет служебной разметки идут последними
    и приоритету пользовательского промпта не подчиняются.
    """
    custom = (rag_override or "").strip()
    if custom:
        block = "\n\n".join(
            [
                custom,
                _rule("entity_prompt_precedence"),
                _policy_rules(),
                mandatory_output_rules(),
            ]
        )
    else:
        block = build_rag_user_instruction()
    if not existing or not str(existing).strip():
        return block
    ex = str(existing).strip()
    if block in ex:
        return ex
    if (not custom) and (
        "только предоставленный контекст (CONTEXT)" in ex
        or "Правила ответа по CONTEXT" in ex
    ):
        return ex
    return f"{ex}\n\n{block}"

def prompts_overview() -> Dict[str, object]:
    """Что реально применилось — для стартового лога и диагностики.

    Отвечает на вопрос «промпт правили в ConfigMap, а он точно доехал?»:
    показывает, какие тексты взяты из config.yml, а какие остались дефолтными.
    """
    return {
        "strict_answer_mode": _strict_answer_mode(),
        "cite_sources": cite_sources_enabled(),
        "answer_summary": answer_summary_enabled(),
        "answer_gaps_note": answer_gaps_note_enabled(),
        "texts_from_config": sorted(
            key for key in _DEFAULT_TEXTS if _config_text(key) is not None
        ),
        "texts_from_defaults": sorted(
            key for key in _DEFAULT_TEXTS if _config_text(key) is None
        ),
    }