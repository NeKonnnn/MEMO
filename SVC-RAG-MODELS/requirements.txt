# SVC-RAG-MODELS: Embedding + Reranker (MiniLM-L12, MiniLM-L6)

fastapi>=0.115.0
starlette>=0.49.1
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pyyaml>=6.0.2

# Embedding and Reranker (sentence-transformers)
sentence-transformers>=3.0.0
# Giga-Embeddings-instruct: авторы рекомендуют >=4.46; config.json — 4.51.0
transformers>=4.46.0,<5.0.0
# Giga-Embeddings-instruct (trust_remote_code) требует einops
einops>=0.7.0

# PyTorch
torch>=2.0.0

# Scientific
numpy>=1.24.3,<2.0.0
